using System;
using System.Collections.Generic;
using System.Text;
using System.Xml;
using System.Net.Sockets;
using System.Net;

namespace InternetStandards.Ecma.Xmpp
{
    public class XmppClient
    {
        
        /* TcpClient client = null;
        NetworkStream networkStream = null;
        XmlWriter writer = null;
        XmlReader reader = null;

        public void Connect(JabberIdentifier jID)
        {
            Connect(jID, jID.Domain, 5222);
        }

        public void Connect(JabberIdentifier jID, string host, int port)
        {
        }

        public void Connect(JabberIdentifier jID, IPAddress ipAddress, int port)
        {
            client = new TcpClient(AddressFamily.InterNetwork);
            client.BeginConnect("xmpp.j.porter.name", 5222, new AsyncCallback(ConnectCallback), null);
        }

        public void OpenStream()
        {
            networkStream
        }

        public void CloseStream()
        {
            wr
        } */



    }
}
